# Lore architecture map

_Generated by Lore. Use this to find which files/areas a change concerns before reading code._

- Backend — MCP server (server.py) that defines and serves tools/handlers, orchestrating Stripe operations for AI clients
- Integrations — Connects to the external Stripe API for payments/billing; LiteLLM used for LLM calls
- CLI — stripe-mcp command-line entry point (__main__.py) that boots and runs the server
